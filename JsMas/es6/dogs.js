export const dogs=['st','mus','bus']
export const dogsCount=dogs.length

//export default dogs; //for single expressen
//but above we have two so use export keyword for both and curly bracket on import