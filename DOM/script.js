//where does the dom come from
  //A. browser gives us dom it gives us access to the dom and certain unique methods and properties

//DOM
  //Document object Model
  //Everything in your browser can be represneted as an object
  //The Dom will map our html into a hierarchical structure
//who is in top of hierarchy WINDOW OBJECT
  //each tab has its own window instance
  //it has many properties
  //closed,frames,history,location,document...etc..
  //if you want to access any elemenet in an html page you always start with accessing the htm element

  //document
  //console.dir(document)